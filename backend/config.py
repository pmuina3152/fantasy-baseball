# ── Configurable constants ────────────────────────────────────────────────────

# Minimum qualifying thresholds
MIN_AB: int = 100       # at-bats required to appear in hitter pool
MIN_IP: float = 20.0    # innings pitched required to appear in pitcher pool

# Cache settings
CACHE_MAX_AGE_HOURS: int = 12
CACHE_DIR: str = "cache"

# Default season
DEFAULT_SEASON: int = 2025
